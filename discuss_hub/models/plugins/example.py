import logging

_logger = logging.getLogger(__name__)


class Plugin:
    """This is an example plugin where some tests run.
    It should not be used in production, but in to validate all the
    functionality of the connector.
    """
